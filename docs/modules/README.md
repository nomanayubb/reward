# Module Documentation

Every module owns a contract in `apps/<module>/README.md` (purpose, inputs,
outputs, dependencies, events, tables, APIs, security, business rules, tests).

Deeper guides for complex modules live here:

| Guide | Module |
| --- | --- |
| (to be added) | offers eligibility engine |
| (to be added) | reward engine configuration |
| (to be added) | withdrawal review workflow |
| (to be added) | fraud rules reference |

Start with the module README — only read this folder when the module contract
is not enough.
